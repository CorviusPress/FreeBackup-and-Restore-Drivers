# FreeBackup-and-Restore-Drivers
FreeBackup and Restore Drivers is PowerShell tool that backs up and restores Windows drivers using pnputil. It exports all installed drivers to a safe location and allows quick recovery after reinstalling or migrating the system.
